{
  "name": "mcp-tool-deps",
  "version": "0.2.0",
  "description": "Dependency graph builder for MCP server tool catalogs",
  "type": "module",
  "scripts": {
    "fetch": "bun src/fetch.ts",
    "build": "bun src/build.ts",
    "serve": "bun src/serve.ts",
    "skill": "bun src/fetch.ts && bun src/build.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.29.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.10",
    "@types/bun": "^1.3.11"
  }
}
